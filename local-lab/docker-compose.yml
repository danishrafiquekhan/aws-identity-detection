services:
  localstack:
    image: localstack/localstack:3.8
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,iam,sts,cloudtrail,logs,ec2
      - DEBUG=0
    volumes:
      - localstack-data:/var/lib/localstack

volumes:
  localstack-data:
